{
  "name": "freelancer-engine",
  "version": "1.0.0",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "build": "mkdir -p dist && echo 'API Engine Active' > dist/index.html"
  }
}
